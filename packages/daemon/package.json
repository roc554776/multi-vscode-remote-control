{
  "name": "multi-vscode-daemon",
  "version": "0.1.0",
  "description": "Multi VSCode Remote Control Daemon",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "multi-vscode-daemon": "./dist/index.js"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "test": "npm run build && node --test dist/__tests__/*.test.js dist/__tests__/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0",
    "zod": "^3.22.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
